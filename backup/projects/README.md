# Projects

子项目按 AI 领域分类。

| 目录 | 领域 |
|------|------|
| `llm/` | 大语言模型（Transformer, GPT, RAG, Agent） |
| `vision/` | 计算机视觉（CNN, ViT, Diffusion） |
| `audio/` | 语音（ASR, TTS） |
| `multimodal/` | 多模态（CLIP, LLaVA） |
| `agents/` | AI Agent（Tool Use, Multi-Agent） |

每个子项目用 uv 管理依赖，独立 `pyproject.toml`。
