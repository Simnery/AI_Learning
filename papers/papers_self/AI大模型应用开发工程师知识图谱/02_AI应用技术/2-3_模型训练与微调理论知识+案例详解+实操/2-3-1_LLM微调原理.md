# 2.3.1 LLM微调原理

> 2.3 模型训练与微调理论知识+案例详解+实操 > 2. AI应用技术

---

## 核心概念

### 什么是 LLM 微调

**一句话**：微调是在预训练模型的基础上，用少量高质量数据"二次训练"，让模型适应特定领域或任务风格。

```
预训练(Pre-training):   万亿 token 通用数据 → 学会"通用语言能力"
微调(Fine-tuning):      万-百万条 专业数据 → 学会"特定领域能力"

类比:
  预训练 = 大学本科教育(通识教育，什么都学一点)
  微调   = 入职培训(在通识基础上，快读掌握专业工作技能)
```

### 为什么需要微调

| 场景 | RAG 能做吗 | 微调的价值 |
|------|:--------:|-----------|
| 学习特定写作风格 | ❌ | 让模型内化"公司报告模板"的文风 |
| 掌握专业术语体系 | 部分 | 让模型理解"法律判例引用规范" |
| 强化某种推理模式 | ❌ | 让模型习惯"先分析再结论"的思维链 |
| 知识密集型问答 | ✅ | RAG 更好（知识可更新） |
| 降低模型幻觉率 | 部分 | 微调领域数据后幻觉明显减少 |

**核心判断**：微调改变的是"思维方式"，RAG 补充的是"知识内容"。

### 前置知识

- LLM 基础（[1.2.1 AI大模型基本原理及API使用](../../01_AI大模型基础/1-2_实操基础/1-2-1_AI大模型基本原理及API使用.md)）
- 线性代数基础（矩阵乘法、矩阵分解的概念）

---

## 原理讲解

### 全量微调 vs 高效微调

| 方式 | 做法 | 显存需求 | 训练时间 | 效果 |
|------|------|:--------:|:--------:|:----:|
| **全量微调** | 更新所有参数 | 模型 ×16-20 倍 | 慢 | 最好 |
| **LoRA** | 只训练"旁路矩阵" | 模型 ×1.2-2 倍 | 快 | 接近全量 |
| **QLoRA** | LoRA + 4-bit 量化 | 模型 ×0.5-1 倍 | 较快 | 略低于 LoRA |

**LoRA 为什么能这么省**：不是去改模型的 700 亿参数，而是在旁边加了一组"薄矩阵"（只有原来的 0.1%-1% 参数），只训练这几层薄矩阵。

### LoRA 的数学原理（用人话讲）

**核心假设**：模型需要"微调"的变化，可以用远比原矩阵小的两个矩阵乘积来表示。

```
原始的大参数矩阵 W（比如 4096 × 4096）
  ↓ LoRA 不直接改 W
  ↓ 而是在旁边加:
  A 矩阵（4096 × r） + B 矩阵（r × 4096）
  其中 r << 4096（比如 r=8,16,64）

模型输出 = W·x + B·A·x
           ↑        ↑
         冻结不训练  只训练这小部分
```

**这个假设的直觉**：

```
类比: "猜你喜欢"
  Netflix 有 1 亿用户 × 10 万部电影 → 万亿级别的矩阵
  但真正决定用户喜好的只有很少几个"隐藏因素"（喜欢动作/科幻/剧情）
  → 用矩阵分解把这些隐藏因素提取出来

LoRA 的 Lo 部分: Low-Rank = 低秩 = 背后的隐藏因素很少
  "模型从通用能力迁移到特定能力需要的改动"
  可以用很少的隐藏维度（r=8~64）来表达
```

### SVD 矩阵分解

SVD（奇异值分解）是 LoRA 的数学基础：

```
任意 m×n 矩阵 M 可以分解为:
  M = U × Σ × V^T

  其中:
    U: m×m 正交矩阵（左奇异向量）
    Σ: m×n 对角矩阵（奇异值，从大到小排列）
    V: n×n 正交矩阵（右奇异向量）

关键洞察: Σ 中的奇异值衰减很快
  → 前 8-64 个奇异值就已经捕获了 >95% 的信息
  → 所以用 r=8~64 的 LoRA 秩就够了
```

### 微调数据准备

**数据质量 > 数据数量**：

```
错误观念: "我有 100 万条对话数据，全用来微调"
正确做法: "精选 5000 条高质量对话，效果可能更好"

经验法则:
  - 风格迁移: 500-1000 条
  - 领域适应: 5000-10000 条
  - 能力注入: 10000-50000 条
```

**数据格式（对话式）**：

```json
{
  "messages": [
    {"role": "system", "content": "你是一个法律助理"},
    {"role": "user", "content": "合同里的不可抗力条款怎么写？"},
    {"role": "assistant", "content": "不可抗力条款应当包含..."}
  ]
}
```

### 显存估算

**核心公式**：

```
微调显存 ≈ 模型加载 + 优化器状态 + 激活值

全量微调 7B 模型:
  模型参数: 7B × 2 bytes(FP16) = 14GB
  梯度: 7B × 2 bytes = 14GB
  优化器(Adam): 7B × (4+4) bytes = 56GB
  激活值: ~10-20GB
  → 总计约 100GB ≈ 需要 4×A100(80GB)

LoRA 微调 7B 模型:
  模型参数: 7B × 2 bytes(FP16) = 14GB
  LoRA 参数(仅 r=16): 7B × 0.2% × 2 = 0.028GB
  梯度(仅 LoRA): 0.028GB × 2 = 0.056GB
  优化器(仅 LoRA): 0.028GB × 8 = 0.22GB
  激活值: ~5-10GB
  → 总计约 25GB ≈ 1×A100(40GB) 或 1×4090(24GB, QLoRA)
```

**显存可能不够时的优化顺序**：
1. 梯度检查点（Gradient Checkpointing）——用计算换显存
2. QLoRA（4-bit 量化）—— 显存减到 1/4
3. 减小 batch_size + 梯度累积

---

## 代码实战

### 环境准备

```bash
pip install transformers peft datasets accelerate bitsandbytes
```

### 实战 1：LoRA 微调最小示例

```python
"""用 LoRA 微调 Qwen2.5-0.5B（消费级 GPU 可跑）"""
from transformers import (
    AutoModelForCausalLM, AutoTokenizer,
    TrainingArguments, Trainer, DataCollatorForSeq2Seq
)
from peft import LoraConfig, get_peft_model, TaskType
from datasets import Dataset

# ===== 1. 准备微调数据 =====
# 最小可跑: 3 条数据演示流程
training_data = [
    {
        "instruction": "将以下句子翻译成更正式的表达",
        "input": "这个功能挺好用的",
        "output": "该功能具有较好的用户体验和实用性。"
    },
    {
        "instruction": "将以下句子翻译成更正式的表达",
        "input": "我觉得这个方案不太行",
        "output": "经评估，该方案存在一定的局限性，建议进一步优化。"
    },
    {
        "instruction": "将以下句子翻译成更正式的表达",
        "input": "你们做的这个太慢了",
        "output": "当前系统的响应速度有优化空间，建议从性能层面进行改进。"
    },
]

def format_example(example):
    return {
        "text": f"<|im_start|>system\n你是文本润色助手<|im_end|>\n"
                f"<|im_start|>user\n{example['instruction']}:{example['input']}<|im_end|>\n"
                f"<|im_start|>assistant\n{example['output']}<|im_end|>"
    }

dataset = Dataset.from_list([format_example(e) for e in training_data])

# ===== 2. 加载模型（使用小模型演示） =====
MODEL_NAME = "Qwen/Qwen2.5-0.5B"  # 0.5B 参数，消费级 GPU 可跑

# 先检查显存: nvidia-smi
# 如果显存不够，使用 4-bit 量化:
# from transformers import BitsAndBytesConfig
# bnb_config = BitsAndBytesConfig(
#     load_in_4bit=True,
#     bnb_4bit_compute_dtype=torch.float16,
# )

print(f"加载模型: {MODEL_NAME}")
print("(如果显存不够，请取消注释上面的 4-bit 量化配置)")

# 实际训练代码（需 GPU）:
"""
import torch
model = AutoModelForCausalLM.from_pretrained(
    MODEL_NAME,
    torch_dtype=torch.float16,
    device_map="auto",
    # quantization_config=bnb_config,  # 显存不够时取消注释
)
tokenizer = AutoTokenizer.from_pretrained(MODEL_NAME)
tokenizer.pad_token = tokenizer.eos_token

# ===== 3. 配置 LoRA =====
lora_config = LoraConfig(
    task_type=TaskType.CAUSAL_LM,
    r=8,               # LoRA 秩（隐藏维度数）
    lora_alpha=16,     # LoRA 缩放系数
    lora_dropout=0.1,  # Dropout 防止过拟合
    target_modules=[   # 哪些层加 LoRA
        "q_proj", "k_proj", "v_proj", "o_proj",
        "gate_proj", "up_proj", "down_proj",
    ],
)

model = get_peft_model(model, lora_config)
model.print_trainable_parameters()
# 预期输出: trainable params: 2,359,296 || all params: 494,032,768 || trainable%: 0.48%
# 只有 0.48% 的参数需要训练！

# ===== 4. Tokenize 数据 =====
def tokenize(examples):
    result = tokenizer(
        examples["text"],
        truncation=True,
        max_length=512,
        padding=False,
    )
    result["labels"] = result["input_ids"].copy()
    return result

tokenized_dataset = dataset.map(tokenize, batched=False)

# ===== 5. 训练 =====
training_args = TrainingArguments(
    output_dir="./lora_finetuned",
    num_train_epochs=3,
    per_device_train_batch_size=1,
    gradient_accumulation_steps=4,  # 模拟更大的 batch
    learning_rate=2e-4,
    fp16=True,                      # 混合精度节省显存
    logging_steps=1,
    save_strategy="epoch",
)

trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=tokenized_dataset,
    data_collator=DataCollatorForSeq2Seq(tokenizer, model=model),
)

print("开始训练...")
trainer.train()

# ===== 6. 保存 =====
model.save_pretrained("./lora_adapter")
tokenizer.save_pretrained("./lora_adapter")
print("LoRA 适配器已保存到 ./lora_adapter")
"""

# 实际使用时取消注释上面的代码
print("以上为 LoRA 微调完整流程代码，实际运行需 GPU")
print(f"训练数据: {len(training_data)} 条")
print(f"LoRA 配置: r=8, alpha=16, 训练参数占比 < 1%")
print(f"预期显存: ~8-10GB(FP16) 或 ~4GB(QLoRA 4-bit)")
```

### 实战 2：显存估算工具

```python
"""微调显存估算"""
def estimate_finetune_memory(
    model_params_b,  # 模型参数量(B)
    train_mode="lora",  # "full" / "lora" / "qlora"
    lora_rank=16,
    batch_size=1,
    seq_length=2048,
):
    """估算微调所需显存"""
    bytes_per_param = 2  # FP16 = 2 bytes

    # 模型加载
    model_mem = model_params_b * bytes_per_param

    if train_mode == "full":
        # 全量微调: 模型 + 梯度 + 优化器(Adam=8 bytes/param) + 激活值
        grad_mem = model_params_b * bytes_per_param
        optimizer_mem = model_params_b * 8
        activation_mem = batch_size * seq_length * model_params_b * 0.001  # 粗略估算
        total = model_mem + grad_mem + optimizer_mem + activation_mem

    elif train_mode == "lora":
        # LoRA: 模型(冻结) + LoRA参数 + LoRA梯度 + LoRA优化器 + 激活值
        lora_params = model_params_b * (lora_rank / 4096) * 2  # LoRA 参数占比
        grad_mem = lora_params * bytes_per_param
        optimizer_mem = lora_params * 8
        activation_mem = batch_size * seq_length * model_params_b * 0.0005
        total = model_mem + lora_params * bytes_per_param + grad_mem + optimizer_mem + activation_mem

    elif train_mode == "qlora":
        # QLoRA: 模型(4-bit) + LoRA + 激活值
        model_mem = model_params_b * 0.5  # 4-bit ≈ 0.5 bytes/param
        lora_params = model_params_b * (lora_rank / 4096) * 2
        grad_mem = lora_params * bytes_per_param
        optimizer_mem = lora_params * 8
        activation_mem = batch_size * seq_length * model_params_b * 0.0003
        total = model_mem + lora_params * bytes_per_param + grad_mem + optimizer_mem + activation_mem

    return total

# 估算对比
models = {
    "Qwen2.5-0.5B": 0.5,
    "Qwen2.5-7B": 7,
    "DeepSeek-V3": 671,
}

print("=== 微调显存估算 ===\n")
print(f"{'模型':<20} {'全量微调':>12} {'LoRA':>12} {'QLoRA':>12}")
print("-" * 56)
for name, params in models.items():
    full = estimate_finetune_memory(params, "full")
    lora = estimate_finetune_memory(params, "lora")
    qlora = estimate_finetune_memory(params, "qlora")
    print(f"{name:<20} {full:>8.1f}GB {lora:>8.1f}GB {qlora:>8.1f}GB")

print("\n结论:")
print("- 0.5B 模型: 单张 4090(24GB) 可 LoRA/QLoRA")
print("- 7B 模型: 单张 A100(40GB) 可 LoRA, 单张 4090 可 QLoRA")
print("- DeepSeek-V3(671B): 即使 QLoRA 也需要多张 A100")
```

### 实战 3：微调后模型评估

```python
"""微调前后效果对比评估"""
print("""
# === 微调评估方法 ===

### 1. 自动评估
from transformers import AutoModelForCausalLM, AutoTokenizer
from peft import PeftModel
import torch

# 加载基座模型 + LoRA 适配器
base_model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen2.5-0.5B",
    torch_dtype=torch.float16,
    device_map="auto",
)
model = PeftModel.from_pretrained(base_model, "./lora_adapter")
tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen2.5-0.5B")

# 测试用例
test_cases = [
    {
        "input": "这个功能挺好用的",
        "expected_style": "formal",  # 应输出正式表达
    },
    {
        "input": "代码有bug",
        "expected_style": "formal",
    },
]

def evaluate(model, tokenizer, test_cases):
    '''简单评估'''
    for case in test_cases:
        prompt = f"将以下句子翻译成更正式的表达:{case['input']}"
        inputs = tokenizer(prompt, return_tensors="pt").to(model.device)
        outputs = model.generate(**inputs, max_new_tokens=100)
        result = tokenizer.decode(outputs[0], skip_special_tokens=True)
        print(f"输入: {case['input']}")
        print(f"输出: {result}")
        print()

### 2. 评估维度
# - BLEU/ROUGE: 与参考答案的 n-gram 重叠度
# - 人工评估: 1-5 分评分（流畅度、准确性、风格一致性）
# - LLM-as-Judge: 用 GPT-4/Claude 对微调前后输出打分
# - 业务指标: 客服场景的满意度、代码场景的通过率

### 3. 过拟合检测
# 微调后模型在训练数据上完美但在新数据上变差 = 过拟合
# 解决方案: 减少 epoch、增加数据多样性、增大 LoRA dropout
""")
```

---

### 预期输出

运行上述代码后，终端应看到类似以下结果（具体数值因环境与输入而异）：

```
（示例）关键日志/打印行出现，且无 Traceback
任务状态: success
耗时: <N> ms
```

若报错，优先检查：依赖是否安装、API Key/本地服务是否可用、路径与 Python 版本是否匹配。

## 进阶方向

### LoRA 的局限性

1. **不能引入全新知识**：LoRA 只调整模型的表达方式，不会"学会"预训练中完全没见过的事实
2. **r 值的选择没有通用标准**：太小的 r 可能不够表达，太大的 r 失去了高效的意义
3. **target_modules 需要经验**：不同模型架构的注意力层名称不同，选错了效果大打折扣

### 工业界微调最佳实践

```
步骤 1: 先试 RAG → 确定 RAG 无法解决（风格/推理/格式问题）
步骤 2: 小规模试跑（QLoRA + 500 条数据）→ 快速验证方向
步骤 3: 分析 Bad Case → 调整数据质量和配比
步骤 4: 加大数据量 + 调高 LoRA rank → 正式微调
步骤 5: A/B 测试 → 对比微调 vs 基座模型效果
步骤 6: 持续收集反馈 → 迭代微调（每月或每季度）
```

### 后续学习路径

- [2.3.2 高质量微调数据工程与评估](2-3-2_高质量微调数据工程与评估.md) — 微调数据的质量和评估
- [2.3.3 LLM模型蒸馏与微调实操](2-3-3_LLM模型蒸馏与微调实操.md) — 完整的微调实操
- [3.5.1 PyTorch核心概念](../../03_AI框架及工具平台/3-5_PyTorch与视觉检测/3-5-1_PyTorch核心概念.md) — PyTorch 训练基础

---

## 常见问题

### 小白最常踩的 3 个坑

1. **用太少太单一的数据微调**
   - 错误：只准备 50 条数据（全是一个模板改的），微调后模型只会回答这一种格式
   - 正确：至少 500-1000 条多样化数据，覆盖不同场景、不同表述方式

2. **不检查 tokenizer 的一致性**
   - 错误：基座模型和 LoRA 适配器用了不同的 tokenizer
   - 正确：LoRA 适配器保存时同时保存 tokenizer，加载时从同一目录加载

3. **以为微调完了就完事了**
   - 错误：微调完不评估，直接上线
   - 正确：用测试集评估 + LLM-as-Judge 打分 + 人工抽查 Bad Case。微调是迭代过程，不是一次性操作

### 自检题

**Q1**：LoRA 的 "r" 参数本质上控制什么？r=8 和 r=64 的差别是什么？

> **答案**：r 控制 LoRA 矩阵的"秩"——即用于表达微调变化的隐藏维度的数量。r 越大，LoRA 的表达能力越强（能捕获更复杂的变化模式），但参数量和训练成本也更高。r=8 适合风格迁移等简单任务，r=64 适合需要较大行为改变的复杂任务。实践中 r=8-32 通常足够。

**Q2**：微调可以让模型学到预训练中没有的"新知识"吗？

> **答案**：很难。微调主要改变模型的"行为模式"（风格、格式、推理方式），而非注入大量新事实知识。如果微调数据包含预训练中完全没有的新信息，模型可能"记住了答案"但无法泛化（类似学生在考试前死记硬背而没有真正理解）。给模型补充新知识应该用 RAG 而非微调。

**Q3**：QLoRA 为什么能做到 4-bit 精度但效果只比 LoRA 差一点点？

> **答案**：QLoRA 在反量化（dequantization）时会先把 4-bit 权重恢复为 16-bit 再计算，前向传播的计算精度几乎无损。同时引入了 NF4（NormalFloat4）数据格式——它不是均匀量化，而是根据神经网络权重的正态分布优化了量化区间，在参数的"高密度区域"分得更细。所以虽然存储是 4-bit，但计算的精度损失很小。

---

## 延伸阅读

### 中文资料（推荐，无需科学上网）

- [LoRA 论文精读 - 知乎](https://docs.python.org/3/) — LoRA 原理解析，从 SVD 到实战
- [QLoRA 高效微调指南](https://huggingface.co/blog/zh/4bit-transformers-bitsandbytes) — HuggingFace 官方 QLoRA 中文教程
- [PEFT 库使用文档](https://huggingface.co/docs/peft/zh/index) — HuggingFace 高效微调库

### 英文资料（可能需科学上网）

- [LoRA 论文](https://arxiv.org/abs/2106.09685) — LoRA 原始论文（Microsoft）
- [QLoRA 论文](https://arxiv.org/abs/2305.14314) — QLoRA 论文，4-bit 微调方法
- [HuggingFace PEFT](https://github.com/huggingface/peft) — 官方高效微调库源码
